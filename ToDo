@relations
@schema validation
@add graphql types
@add types validation
@create inputs
@add relation to inputs
@add input validation
@add graphql queries
@add graphql mutations(update)
@add query,sort,pagination
@computed values in app(paid,leftToPay)
@add validation to payment(amount) tho check amount not bigger than course price
@authentication
@add users script
@add logs
#todo analitics
#todo add clusters
