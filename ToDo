@relations
@schema validation
#####todo add graphql types
#####todo add graphql mutations&queries
###todo add graphql validation
#####todo authentication
#####todo authorization
###todo computed values in app(paid,leftToPay)
##todo add validation to date fields(task(dateExec)) to check they are not smaller current date
#todo add validation to payment(amount) tho check amount not bigger than course price
#todo add clusters
#todo add logs